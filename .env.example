# Supabase public project values. The publishable key is designed for browser/server app use with RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Local development origin. Production uses the incoming Cloudflare request origin for OAuth redirects.
NEXT_PUBLIC_APP_URL=http://localhost:3000
