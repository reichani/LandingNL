# LandingNL's production Supabase URL and publishable browser key are canonicalized in
# src/lib/supabase/public-config.ts to avoid Cloudflare build/runtime drift.
# Local overrides are intentionally not required for the MVP production path.

# Local development origin. Production OAuth redirects use the incoming request origin.
NEXT_PUBLIC_APP_URL=http://localhost:3000
